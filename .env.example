# 本地开发密钥示例（不要把真实密钥提交到仓库）
# 可复制为环境变量，或写入已 gitignore 的 appsettings.Development.json

# Auth__JwtSecret=your-long-random-secret
# AUTH_JWT_SECRET=your-long-random-secret
# ConnectionStrings__Oracle=User Id=CAT_SYSTEM;Password=***;Data Source=localhost:1521/FREEPDB1
